---
const {project} = Astro.props
const {name, img} = project
---
<div class="bg-light-blue rounded-lg w-80 h-auto p-4 m-4 flex flex-col justify-center items-center gap-2">
  <h3 class="text-center text-xl">Proyecto: <span class="font-bold">{name}</span></h3>
  <img src={img} alt={`Logo de ${name}`}>  
</div>